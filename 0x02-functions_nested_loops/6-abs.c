#include "main.h"

/**
  *_abs - function that computes the absolute value
  *of an integer.
  *
  *@n: The int to print.
  *Return: Absolute value
  */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}

	else
	{
		return (num);
	}
}
