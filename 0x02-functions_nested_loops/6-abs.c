#include "main.h"

/**
  *_abs - function that computes the absolute value
  *of an integer.
  *
  *@n: The int to print.
  *Return: Absolute value
  */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	else
	{
		return (n);
	}
}
