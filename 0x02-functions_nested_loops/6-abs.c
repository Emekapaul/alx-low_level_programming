#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @num: The character to print.
 *
 * Return: on success returns int.
 */
int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
